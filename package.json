{
  "name": "@inhuman.tools/arrival-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "The arrival language — an R7RS-subset Scheme for AI-agent constraint-based execution: interpreter, capability environments, provenance, MCP tooling, the CLI, and the Mercury compiler stack",
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.9.0"
  },
  "packageManager": "pnpm@10.3.0",
  "engines": {
    "node": ">=22"
  }
}
