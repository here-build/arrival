frame door: `abs`, `join`, `min`, `number->string`, `reverse` are not supported by the runtime module yet — no manifest export exists. Add the shim + its STAGE0 manifest row in src/runtime/stage0.ts (constitution §4.4; the export set grows corpus-driven).
