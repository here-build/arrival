import { error } from "./stage0.mts";
function OracleMain() {
    const __or = false;
    return __or !== false ? __or : error("does-run");
}
export { __oracleResult };
const __oracleResult = OracleMain();
