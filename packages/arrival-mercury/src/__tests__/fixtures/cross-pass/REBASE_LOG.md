# Cross-pass fixtures — rebase log

Constitution §9 golden discipline: a byte-change to any `*.golden.json` file in
this directory requires an entry here. "Re-base once, explicitly" is a
mechanism (this log), not a culture instruction.

## goldenEpoch 1 — initial baseline (2026-07-14)

First-landing baseline for the eight representative corpus rows named by the
cross-pass-fixtures mission (constitution §5.3): `truthy-zero-then`,
`filter-truthy-zero`, `modulo-neg`, `multi-list-map`, `or-in-define`,
`quotient-neg`, `apply-plus`, `member-assoc`.

Generated by running the real `extractFacts` → `walk` → `render` slice (the
same public building blocks `cross-pass-fixtures.test.ts` itself calls — a
harvested registry via `emitRegistryOf` + `withRules(phase1Rules)`, one shared
`OracleSession`) against each row's committed `corpus/<row>.scm` source, and
committing the observed output verbatim — no hand-typed facts or residual
bytes. Not derived from `compileGreenfield` (the oracle's own wrapped
pipeline stays black-box; see the test file's header for the "two tests, one
corpus, two owners" framing).

No prior baseline existed — this is Gate 2's cross-pass-fixture obligation
landing for the first time, not a rebase of a previous epoch.
