import type { ExpectedOutcome } from "../../index.js";

/** `apply` over the variadic `+`: a plain reduce/fold — `6` (Appendix B: representation-collapsed). */
export const expected: ExpectedOutcome = { value: 6 };
