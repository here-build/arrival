// `@inhuman.tools/arrival/capabilities/schema` — opt-in `s/*` scheme type vocabulary.
// Also pulled transitively as `overridableCapability`'s dep; arm alone for bare `s/*` without params.
export { schemaCapability } from "../env/schema/schema.js";
