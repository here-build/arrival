// attestation-surface — curated face for manifold `s/*` boundary reads.
// `attestDeep` lives on `/reflect-internals` (deep-walks value classes; verdict consumers).
// Definitions remain in `values/attestation.ts`; this barrel only narrows re-exports.
export { attest, isAttested, freshIfSingleton } from "../attestation.js";
