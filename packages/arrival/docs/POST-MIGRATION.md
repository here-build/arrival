# Post-migration ledger — downstream breakage parked during the core rework

*Rule (V, 2026-07-09): downstream packages are already partially broken from prior API
reworks. Anything deeply wrong discovered in a downstream package or its tests during the
[`REWORK-DAG.md`](REWORK-DAG.md) execution gets a row HERE and work continues — it never
blocks a core commit. This phase runs after node Z.*

Row format: package · what's broken · which rework node exposed it · suspected depth
(import-fix / test-rot / design-mismatch).

| Package | Breakage | Exposed by | Depth |
|---|---|---|---|
| `arrival-provenance` | ~~becomes a re-export shim at C0~~ DONE 2026-07-08: analysis layer moved into `arrival/src/provenance/`, package is now a pure re-export shim (`.` + `/analysis`, both routed through `@here.build/arrival/provenance`). All direct consumers (second-foundation/{arrival-run,arrival-form-lens,arrival-effects,arrival-program-analysis,arrival-chain,arrival-reflect}, inhuman/{saas/mcp,saas/api,public-packages/inhuman}) typecheck 0 through the shim — verified, not just planned. DISPOSITION (audited + finalized 2026-07-10, per V's merge directive): the merge is FINAL as-is — the package deliberately survives as the quarantined mobx home of `ObservableEvalTrace` (core stays mobx-free per P12; a separate package to isolate an external dep is exactly the env-quasi-packages cut) plus the `@here.build/arrival-provenance` name serving ~145 import sites. Its remainder is NOT relocated into core — `EvalTrace` is the §1-EXCLUDED representation; moving it into core right before rows 27/28 re-ground its consumers would plant scheduled-to-die code in core. The package deletes wholesale when the reflect wave (row 27) + elk wave (row 28) land and the last `EvalTrace` construction site dies. Dead-facade cleanup done 2026-07-10: `src/{scope-id,trace-snapshot}.ts` (orphaned re-export files, zero importers) deleted; `snapshotTrace` now re-exported from core directly in `index.ts`; public surface unchanged, build 0. | C0 (landed) | — |
| `inhuman/sift-submission/runner`, `inhuman/sift-submission/mcp` | `AValue.toJs` missing (renamed/moved?), `strict-row.ts` `AJSObject.get`/`withProvenance`/`RunContext` shape mismatches, `SiftSymbolDef.withContext` unknown prop, `scheme-zod`'s `.unknown` missing | discovered incidentally while gate-checking C0's downstream consumers; unrelated to provenance — looks like in-flight AValue/RunContext/scheme-zod rework from another track (A2/A4 boolean-mint or D-track type layer) | design-mismatch |
| `inhuman/saas/studio` | `@here.build/arrival-type-lens-tsgo` module not found (`tsgo-lens.worker.ts`) | discovered incidentally while gate-checking C0's downstream consumers; unrelated to provenance | import-fix (missing build/link) |
| `inhuman/examples/ai-winter-thawed` | no `tsconfig.json` in the package — `tsc --noEmit` can't run standalone, no direct verdict possible | discovered incidentally while gate-checking C0's downstream consumers (declares `@here.build/arrival-provenance` as a dep, uses only `.`/`/analysis` names the shim preserves) | test-rot (missing tsconfig) |
| `arrival` core itself | ~~`src/env/r7rs/numeric.ts` 2 tsc errors (`mintVerdict` missing export...)~~ DONE 2026-07-08: two-tier-exec-api.md §8 steps 1-2 landed (`execState`/`ExecState` extracted; `op-helpers.mintVerdict` added + swept through equal?/eqv?/eq?, `wrapOrd`, `applyNumeric`'s deleted raw-boolean fast path, `looseOrderChain`/`looseCompare`, `nativeTypePredicate`, `number?`, and `taglessGuard`'s `run`). `tsc -p tsconfig.build.json --noEmit` is 0 errors. | C0/Track A (landed) | — |
| `arrival` core itself | ~~41 sunset-suite test failures~~ DONE 2026-07-08: rebaselined to 36 (the pre-existing floor, all in files this migration never touched: `attestation`, `contract-precision-fixes`, `env/overridable`, `env/r7rs/{bytevectors,lists}-contract-precision`, `type-layer/{lower,prelude,query,schema-to-ts}` — traced to concurrent scheme-zod/lists.ts/rosetta.ts/overridable.ts rework, zod `Invalid input`/schema-precision/error-message-format failures with no call-path through `op-helpers.ts`/`equality.ts`/`numeric.ts`/`taglessGuard.ts`). The 5 failures the R8 mint sweep itself caused (`coercion-soundness` vector? taglessGuard ×2, `js-interop` boolean-exit ×1, `srfi` vector-empty?/any/every ×2) were genuine test-flips — rebaselined to assert the now-boxed ABool truth, citing R8, per the standing convention (not reverted). | C0/Track A (landed) | — |
| `arrival` core itself | ~~8 sunrise failures in `laws/term-carrier.law.test.ts` (equals element-unioning + conservation)~~ DONE 2026-07-08: `equal?`'s GAPS row ("equal? verdict is empty-provenance flyweight", `ledger/index.law.test.ts`) retired — the two `it.fails` cells flipped to plain `it()`, citing RULINGS R8. Surfaced a SEPARATE, genuinely pre-existing gap while flipping: AJSArray (`borrow-array`'s `fromJS`) and ADict (`dict`'s `new ADict(CONSTANT_CTX, ...)`, `env/polyglot.ts`) never stamp their own top-level provenance with the R2 grouping-fact union at construction — independent of R8, un-implemented (R2 is its own later design item, RULINGS.md). Those 2 carriers' equals cells stay `it.fails`, re-tagged under a NEW ledger row (`"AJSArray/ADict container carries no grouping-fact provenance"`, gate `R2 container-provenance ruling`) rather than the retired R8 one. Full sunrise gate: 995 passed, 157 expected-fail, 0 unexpected reds. | C0/Track A (landed) | — |
| `arrival` core itself | ~~two-tier-exec-api.md §8 step 4: flip `exec` to the simple (plain-JS) tier~~ DONE 2026-07-09: `exec` now `state.values.map(toJS)` (was byte-identical boxed passthrough). Migrated every box-asserting caller found by the sunrise+sunset+conformance gates to `execState` (COMPLEX tier) — `laws/_tables/fixtures.ts`, `laws/term-carrier.law.test.ts`, `_lineage-test-helpers.ts` (cascades to golden-prov-*/lineage-*/`provenance/conservation.law.test.ts`), and ~28 sunset files (`generator-exec.spec.ts`, `tail-call.test.ts`, `polyglot.test.ts`, `attestation.test.ts`, srfi/string/keyword/sandbox/lineage/cxr/projection-nil-tolerance/collapse-provenance test files, `common/{input-rest,kwargs}-runtime.test.ts`, `tap.spec.ts`). Rebaselined (not migrated) where the new plain-JS shape IS the correct assertion: `js-interop.test.ts` (4 `it.fails` promoted to `it()` — BigInt/char/list gaps genuinely closed; the boolean-boxed test inverted to boolean-plain, citing R1), `r7rs-{numbers,unicode,identity}.test.ts` (`num()`/`truthy()` helpers collapsed to their single plain-JS shape, their own documented scheduled inversion), `control-forms-keywords.test.ts`/`evaluator.spec.ts` (bare symbol-print assertions rebaselined to the apostrophe-prefixed ASymbol toJS marker). Found and fixed one REAL production regression the strict `toJS` exit surfaced: `env/r7rs/numeric.ts`'s `number->string` returned a raw, never-boxed JS string via a `symbol.native` binding (a documented, deliberate escape-hatch cast) — any top-level `(number->string …)` call would have crashed `exec`'s new `toJS` invariant; now boxes as `AString` carrying the operands' provenance union. Flipped crossing.law.test.ts's 5 scalar `it.todo` exit cells (boolean/safe-int/float/bigint/string) to live `it()` rows asserting REAL `exec()` output (registered-symbol stays `it.todo` — ASymbol's OWN opaque-exit design-pending deferral, unrelated to R1); added 4 new "via REAL exec output" rows for the R9 container types (list/dotted-pair/vector/dict), which previously only asserted a synthetic `toJS(hand-built-box)` call. Retired the "boolean raw exit via op-helpers short-circuit" `ledger/index.law.test.ts` INVERSIONS row (R1+R8 together close it). Gates: sunrise 1029 passed/0 failed/149 expected-fail (baseline 1021/149, net +4 real passes from js-interop promotions, +4 new container rows, -5 todo from the scalar flips); conformance 651 passed/0 failed (exact match); `tsc -p tsconfig.build.json` 0 errors; full `tsc --noEmit` byte-for-byte identical error set to pre-step-4 baseline; sunset 36 failed — the exact same 36 failing test titles as the pre-step-4 baseline (byte-for-byte `diff`, confirmed twice), 2562 passed (+4 vs baseline's 2558). Downstream `exec()` callers in `arrival-mcp`/`arrival-chain`/second-foundation audited read-only — findings in the rows below. | two-tier-exec-api.md §8 step 4 (landed) | — |
| `arrival-mcp` | `DiscoveryTool.ts:67` `execSerialized()` maps `toSExprString` over `exec()` results — the serializer (`arrival-serializer/src/serializer.ts:156`) dispatches by `obj.constructor?.name === "AExact"/"ASymbol"/"APair"/…`, so with `exec` now exiting plain JS none of those branches fire and MCP tool-result output degrades to generic JS rendering (no Scheme quoting/rationals/`#\c` chars/pair print-form). Fix direction: switch `execSerialized` to `execState` (COMPLEX tier — serialization is a boxed-state read, exactly R1's "tooling" case). `DiscoveryTool.ts:320`'s `evalScheme` callback is unaffected (result discarded). | two-tier-exec-api.md §8 step 4 (`exec` → plain-JS exit) | design-mismatch (a 1-line `execState` switch once picked up) |
| `arrival-manifold` (second-foundation) | Test-only, currently green but COINCIDENTAL: `__tests__/bind.test.ts` (11 sites) does `schemeToJs(result)` on now-already-plain exec output (dead unwrap — `schemeToJs` passes plain values through); `session-declaration-persistence` + `replay-cache-{restore,safety}.test.ts` (~8 sites) assert `String(execResult)` — passes for small ints because `String(1)` ≡ the old boxed print form, would diverge for rationals/floats/out-of-safe-range bigints. Migrate to `execState` or assert the plain-JS value directly when next touched. | two-tier-exec-api.md §8 step 4 | test-rot (latent, green today) |
| `arrival-chain`/`arrival-run`/`arrival-effects`/`arrival-sampler` (second-foundation) | AUDITED CLEAN for the step-4 exec flip: every `exec` call site either discards the result, uses it only as an `evalScheme` callback, checks `.rejects.toThrow`, or does a promise-typeof check — no box-only reads. (`arrival-chain`'s `approval.test.ts:23` `schemeToJs(await exec(...))` is now a dead double-unwrap but stays correct.) | two-tier-exec-api.md §8 step 4 | — (no action) |
| *(append as discovered)* | | | |
| `second-foundation/arrival-run` (run-program.ts:374), `inhuman/saas/studio` (workbench/trace/run-traced.ts:503) | import the deleted `installHeapMeter` — the install-once-on-shared-glass-env pattern was ALREADY dormant (each exec mints its own RunContext from its own `heapBudget` option; env-level meters were never consulted by any charge site). Fix on their side: pass `heapBudget` per exec call (already threaded end-to-end). *(Q21 audit 2026-07-10: still live at both cited lines — `installHeapMeter` has zero definitions left in arrival src.)* | env T0 (a823e402fd) | import-fix (pre-existing dormancy surfaced) |
| `second-foundation/arrival-chain` | 5 call sites (lineage-field-shadow-corpus.test.ts, __research__/e1-nonkeyword-divergence.test.ts) on the retired 2-arg `classifierFromEnv(env, SOURCES)` — needs the 1-arg form + real `.provenanceRole` markers | Q3 (declaration-driven classifier) | import-fix |
| `second-foundation/arrival-reflect` | **The largest hidden production reader of eager stamps** (Q20's own sweep list): all six verbs project `ResultHandle.teleological()`'s EvalTrace — the §1-EXCLUDED per-reduction representation — and under Q20b's default-OFF, trace.ts + the EvalTrace siblings degrade to point-self-ids (empty accumulation). Not broken-crashing, broken-hollow: traces still shape-check, carry no lineage. NAMED CONSUMER WAVE: re-ground the verbs on wireframe-cone × stream joins per `docs/working-proposals/arrival-reflection-env-over-provenance.md` (repo-root docs). | Q20b (16ff612f1b) | design-mismatch (named consumer wave, after Q20) |
| `inhuman/saas/studio` | 13 EvalTrace-consuming files (grep at audit time; the plan's "~20" overcounted) — the elk render pile reads the same §1-EXCLUDED EvalTrace and hollows out identically under Q20b default-OFF. NAMED CONSUMER WAVE: `docs/working-proposals/inhuman-elk-over-provenance.md` (render joins over template graph + ordinal z-space + RLE depth instead). | Q20b (16ff612f1b) | design-mismatch (named consumer wave, after Q20) |
| `arrival` core itself | `src/rosetta.ts`'s `argProvenance` option (line ~312): computes `deepProvenance` per scheme arg — under default-OFF those stamp sets are empty, so any future consumer gets hollow sets. DORMANT: zero live callers at audit time (Q20b's commit message pins this). Fix rides whichever consumer first enables it (force the oracle per-call, or re-ground on records). | Q20b (16ff612f1b) | test-rot (latent — dormant reader, green today) |
| `arrival` core itself | `src/common/symbols/rosetta.ts` pipe-role path: a `"pipe"`-role rosetta FORWARDS the input provenance union instead of minting — under default-OFF the forwarded union is empty, so pipe-rosetta results carry no lineage in production. Q20b named it a FAST-FOLLOW ("lower-traffic, ctx-contended territory"): mint-under-emission or oracle-gate the forward path. | Q20b (16ff612f1b) | design-mismatch (named fast-follow) |
| `arrival` core itself | mux-decision records are never emitted from LIVE runs: `store/emit.ts`'s `emitMuxDecision` has no production caller — nothing at HEAD advances a per-node coordinate during a live run (the wireframe-WALKING DRIVER is future work; `eval/provenance-hooks.ts`'s own header says so). Replay-side D3 derivation covers the law corpus meanwhile (γ rederives decisions), so this blocks no landed law — it blocks live-stream completeness for port-coupled muxes once a consumer reads the stream. | Q16 (8e61e01f8a, named gap in its own commit message) | design-gap (walking-driver wave) |
| `arrival` core itself | dict mints lose per-field stamps — a dict-shaped mint payload keeps its top-level stamps but not per-field attribution, so retrospective field-k cones over dict egress are coarse (adjacent to, not identical with, ledger row "field-shaped pure ops not projection-aware": that one is the PROSPECTIVE sibling-leak, this one is the RETROSPECTIVE payload grain). | Q16 (8e61e01f8a, named gap) | design-gap (rides the same field-representation V ruling as Q9 finding 6) |
| `arrival` core itself | D1 FIFO ingress keying is a STAND-IN: `src/provenance/replay.ts` (header, design call D1) keys frozen ingress as per-op FIFO queues (`FrozenMints`) because no live coordinate advances per-node ordinal paths yet — exact for the law corpus (demand order matches emission order per op, loop-free), AMBIGUOUS for two same-op sources where only one fired. Resolves to spec-shaped (template-hash, ordinal-path, epoch) keying when the walking driver lands. | Q16 (8e61e01f8a, design call D1) | documented LIMIT (stand-in, replaced by walking-driver wave) |
