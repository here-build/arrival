# Test Suite v2 — Removal Manifest

*Everything the consolidation removes, with the invariant value each item carried and where
that value SURVIVES in the new suite. Rule: nothing is deleted until its surviving home is
named — a removal without a survivor row is a coverage regression, not a cleanup.*

Legend: → `laws/` `conformance/` `membrane/` `provenance/` `doors/` `ledger/` are the v2
suites (see DESIGN.md). "gone" = the invariant was a lie/vacuous/duplicate; nothing survives
because nothing true was enforced.

## A. Whole files removed

| File | What it carried | Survivor |
|---|---|---|
| `keyword-syntax.test.ts` | 3 real accessor cases inside exploratory noise | accessor cases → `laws/accessor.law.test.ts` table; vacuous blocks → gone. **EXECUTED 2026-07-09 (G2)**: `laws/accessor.law.test.ts` created (getter, missing-key→nil, map/filter composability, + the co-located pipe-quoted-symbol case not itself an accessor law but preserved rather than lost); `keyword-syntax.test.ts` deleted. Verified 5/5 green under sunrise. |
| `module-composition.spec.ts` | resolver-ordering contract via private `_lookupWithResolvers` | public-altitude row in `laws/env-resolution.law.test.ts` (capability-assembled path); if unreachable publicly, ONE retagged internal unit stays. **EXECUTED (kept, no deletion) — re-verified 2026-07-09 (G2)**: no public-altitude survivor exists (re-checked `capability.test.ts` and `capabilities-assembled.test.ts`, same conclusion as the file's own 2026-07-08 G1 retag comment). File stays as the honest internal-unit floor per its in-place `[RETAG — deliberate internal-module unit suite, not a test-only-API artifact]` note. |
| `clone-identity.test.ts` | nil-clone regression war story; 14-site `=== nil` ledger (majority stale, 2 cited files deleted) | nil-clone-tolerance becomes ONE law row: `laws/identity.law.test.ts` — "every spine/guard treats a provenance Nil clone as nil" × carriers; the war story → `docs/archaeology/nil-clone-sweep.md`. **EXECUTED 2026-07-09 (G2)**: war story already lived at `docs/archaeology/nil-clone-sweep.md` (done by an earlier sweep); `laws/identity.law.test.ts` created carrying every site (witness sanity, membrane.ts, rosetta.ts incl. the one open `it.fails` — now `// @ledger: nil-clone schemeToJs entry loses identity`, list-copy, APair tagless-final map/filter/reduce/traverse, readMember/hasMember); `clone-identity.test.ts` deleted. New GAPS row added to `ledger/index.law.test.ts`. Verified 26 passed/1 expected-fail/1 todo green, walker green. |
| `evaluator-benchmark.spec.ts` | fictional LIPS-vs-generator A/B; real content = exec-seam overhead | `__benchmarks__/exec-seam.bench.ts` honest single benchmark, no A/B. **EXECUTED 2026-07-09 (G2)**: created `__benchmarks__/exec-seam.bench.test.ts` (named `.bench.test.ts`, not literally `.bench.ts`, so it actually matches `vitest.benchmarks.config.ts`'s `{test,spec}` include glob and the checked-in `*.test.ts` naming rule) — three honest nested-cost measurements (full `exec(source)` seam / `execExpr(pre-parsed)` / bare `run(evaluate())` with a resolver+runCtx built once), no "speedup" claimed, no two-evaluators fiction. Old file deleted. Verified 4/4 green. |
| `chibi-r7rs.spec.ts` (v1 harness) | THE value-layer conformance law + registries + anti-vacuity | chibi harness v2 (`conformance/`) — removal gated on v2 reaching registry parity + the >500-pass floor. **EXECUTED 2026-07-09 (G2)**: transcription audit completed (see G2 task report's audit table) — every v1 EXCLUDED_TESTS / EXPECTED_FAILURES / EXCLUDED_GROUPS row accounted for in `chibi/registries.ts` + `chibi/manifest.ts`'s "unreadable" reader-door classification, several IMPROVED (narrower, verified-fixed rows removed, a genuinely new middle-ellipsis-capture bug found independently). `chibi-r7rs.spec.ts` + `chibi-harness.ts` (v1-only, no other importers) deleted. v2 (`conformance/chibi-r7rs-v2.spec.ts`) is the sole survivor, ≥1000 manifest test forms / >500 runnable floor / registry-coherence meta-tests all green. |
| `rosetta-environment.test.ts` | legacy defineRosetta exercises + conversion round-trips | conversions → `membrane/crossing.law.test.ts` tables; defineRosetta rows → `ledger/` `[INVERTS: reverse-membrane]` until the legacy arm dies, then gone. **PARTIALLY EXECUTED 2026-07-09 (G2)**: per B4's 2026-07-09 audit the defineRosetta rows do NOT die yet (gate: McpEnvCapability annotation-lifting, undone) — only the conversion round-trips retired. Ported the one genuinely novel case (symbol-keyed-properties JS↔scheme↔JS regression) and the one design-pending stub (empty-list→empty-array) into `membrane/crossing.law.test.ts`; deleted the "LIPS → JS Conversion" / "JS → LIPS Conversion" / "Rosetta Function Wrapping" describes (subsumed by crossing.law.test.ts's CROSSINGS table, more rigorous). File renamed `rosetta-define-legacy.test.ts` (content is now ONLY the surviving defineRosetta rows) — git-mv'd, not deleted; survives until the McpEnvCapability gate closes. Verified 5/5 green (defineRosetta file) + crossing.law.test.ts still green with the 2 ported cases. |
| `equality-representation.test.ts` | representation-blind equality (transitional tolerance) | ONE table in `laws/equality.law.test.ts` with per-row `[INVERTS: bare-value-purge]` tags; post-purge the rows flip to strict-door throws. **EXECUTED 2026-07-09 (G2)**: A4 (bare-value-purge, landed) already settled the mechanism question per-row — NOT a strict-door throw (AString/ABool's representation-blind Setoid is durable, independently pinned by scheme-string-algebra.test.ts + boolean-landmine-regression.test.ts) — so no `[INVERTS]` tags remained to carry; `git mv` to `laws/equality.law.test.ts`, import paths fixed, header reframed to explain the relocation, cross-references in `equality.ts`/`contract-precision-fixes.test.ts`/`tagless-final-equals.test.ts`/`coercion-soundness.test.ts`/`ledger/index.law.test.ts` updated to the new path. Verified 14 passed/1 todo green. |
| `dataflow-thesis-probes.test.ts` | falsification probes for undecided design (R5) | `ledger/` as it.todo gated on the R5 ruling — a probe of an undecided question is a staged spec, not a green test. **EXECUTED 2026-07-09 (G2)**: R5 confirmed RULED in `RULINGS.md` (both cone queries + the execution-plan wireframe target). Created `ledger/dataflow-cones.todo.test.ts` with 2 `it.todo` stubs (DROP + ATTRIBUTION questions), gated on the C3 wireframe implementation (no wireframe design doc exists in this checkout yet, confirmed by search — the gate is genuinely open, hence `it.todo` not `it.fails`). Old file deleted. Verified ledger dir green (walker + new stubs). |
| `half-baked.test.ts` | AHalfBaked carrier mechanics in isolation (interval narrowing, early decide, force/refine fold) | gone: AHalfBaked itself dissolved (halfbaked-existence-review.md, VERDICT KILL — zero production reachability, superseded by R2/C3 struct-fact wires); the motivating program moved to execution-plan-wireframe.md §8 as a struct-fact-wire acceptance criterion. **ALREADY EXECUTED** (pre-G2, commit 90272a0b99 — confirmed via `git log`, no action needed this pass). |
| `speculative-eval.test.ts` | Tier-2 speculation end-to-end oracle (equivalence + early-collapse-vs-hang) | gone: the equivalence floor became vacuous once the producer wiring died — speculate-on and speculate-off are now the SAME code path by construction, not by proof. **ALREADY EXECUTED** (pre-G2, commit 90272a0b99 — confirmed via `git log`, no action needed this pass). |
| `deferred-value-egress.test.ts` | force-on-egress hazard characterization (a live AHalfBaked crossing exec's boundary) | gone: same VERDICT KILL as above — no carrier can exist, so there is nothing left to force at egress. **ALREADY EXECUTED** (pre-G2, commit 90272a0b99 — confirmed via `git log`, no action needed this pass). |

## B. Blocks/rows removed from surviving files

| Site | What it carried | Survivor |
|---|---|---|
| golden-prov-arithmetic "documented asymmetries" (cdr spine, append) — green pins | the DROP behavior (a lie) | `it.fails` rows in `provenance/conservation.law.test.ts` asserting the CORRECT propagation (P10); flip green when the conservation repair lands. **ALREADY EXECUTED** (conservation repair landed pre-G2 — file's own header confirms "REPAIRED... no longer under 'documented asymmetries'"; re-verified 2026-07-09, no action needed). |
| golden-prov-fan A13 green pin + lineage-assumptions A13/A18b duplicates ×2 | the over-attribution leak ×3 | ONE `it.fails` row in `provenance/conservation.law.test.ts` (count-cone minimality), G2 gate cited. **ALREADY EXECUTED** (A13 interim fix landed pre-G2 — golden-prov-fan's own row now reads "the A13 leak is CLOSED"; lineage-assumptions' duplicates already consolidated to the ONE conservation.law.test.ts row; re-verified 2026-07-09, no action needed). |
| coercion-soundness DR4 vector-map cross-out golden | the box-strip (permanent empty-provenance loss) | `it.fails` row in `laws/term-carrier.law.test.ts`: map preserves boxes × EVERY carrier. **ALREADY EXECUTED** (conservation repair landed pre-G2 — coercion-soundness.test.ts's own row now reads "FIXED: DR4"; ledger's GAPS list confirms retired; re-verified 2026-07-09, no action needed). |
| coercion-soundness container-box drop rows (length/sort/map/filter) | contested P10 container question | R2-gated: ledger rows until ruled, then law rows either way. **ALREADY EXECUTED** (R2 ruled in `RULINGS.md`, C1/C2/C4 batch landed pre-G2 — coercion-soundness.test.ts's own header now reads "RULED (R2)... now the fixed, ruled behavior, asserted directly"; re-verified 2026-07-09, no action needed). |
| deferred-value-egress live-escape green "CHARACTERIZATION" | the leak | `it.fails` in `membrane/egress.law.test.ts` (force-on-egress), existing todos absorbed. **MOOT / ALREADY EXECUTED**: `deferred-value-egress.test.ts` itself is gone (AHalfBaked VERDICT KILL, see the A-section row above) — there is no live carrier left to characterize an egress hazard for. Confirmed via `crossing.law.test.ts`'s own trailing comment ("egress of deferred carriers... retired: AHalfBaked itself dissolved"). No `membrane/egress.law.test.ts` file needed. |
| js-interop exact-JSON + list-JSON green throws | BigInt JSON gap ×2 | `it.fails` rows in `membrane/crossing.law.test.ts`. **ALREADY EXECUTED** (js-interop.test.ts's own rows read "the it.fails gap this row named is closed"; re-verified 2026-07-09, no action needed). |
| js-interop raw-boolean exit row | half of the P4 contradiction | R1-gated: the exit-contract TABLE in `membrane/crossing.law.test.ts` asserts ONE convention for all types. **ALREADY EXECUTED** (R1 landed pre-G2 — js-interop.test.ts's own row reads "PROMOTED (RULINGS.md R1)... R1's uniform plain-JS exit"; crossing.law.test.ts's boolean case asserts the one convention via real `exec` output; re-verified 2026-07-09, no action needed). |
| symbol.test-d OLD-shape rows ×6; env test-d museum rows ×15 (lists 6, vectors 4, strings 2, bytevectors 2, binding 1) | narration of pre-fix shapes | gone (NEW-side rows survive in place; `numeric.test-d`/`polyglot.test-d` are the converged shape). **ALREADY EXECUTED** (symbol.test-d.ts's OLD rows already read "DELETED"; re-verified 2026-07-09, no action needed). |
| clone-identity META ledger (`sites.length === 14`) | documentation | `docs/archaeology/`. **EXECUTED** — see the A-section `clone-identity.test.ts` row (moved with the rest of the file's retirement, 2026-07-09). |
| schema-to-ts `"unknown[] \| unknown[]"` green residual | union-dedup gap | `it.fails` asserting `"unknown[]"`. **EXECUTED 2026-07-09 (G2)**: `src/type-layer/__tests__/schema-to-ts.test.ts` — split the residual assertion out of the shared "prints the rest of the scheme-identity primitives" `it()` into its own `it.fails("prints z.vector(z.value) deduped as 'unknown[]'...")`, citing the existing `ledger/index.law.test.ts` GAPS row "schema-to-ts vector union not deduped". Verified: 1 expected-fail, no new failures (6 pre-existing unrelated failures in this file, confirmed present in the pre-edit baseline too). |
| sandbox-escape weak doors (make-string/make-vector caught-only, nested-parse message, either-outcome cyclic equal?) | DoS caps exist | rewritten rows in `doors/resource-caps.law.test.ts` asserting the TAUGHT message (P5), one committed behavior for cyclic equal? **ALREADY EXECUTED**: `doors/resource-caps.law.test.ts` exists and covers make-string/make-vector/nested-parse with taught-message assertions; sandbox-escape.test.ts's own rows are independently already rewritten ("REWRITE (same sweep/rationale)" comments) to assert the taught message rather than caught-only. Residual duplication between the two files noted as a minor follow-up (not a correctness gap, out of this pass's scope). |
| evaluator.spec hand-AST special-forms duplicates of generator-exec rows | nothing beyond duplication | gone; evaluator-only cases (empty begin, if-without-else, nested-if, closures, rest-params) move into generator-exec string-source tables. **PARTIALLY EXECUTED**: the point-duplicated rows are already deleted in place (`[P15 redundancy] DELETED` comments confirm, pre-G2) — the DUPLICATION is eliminated. The evaluator-only cases still live in evaluator.spec.ts's hand-AST form rather than having physically moved into generator-exec.spec.ts's string-source tables; left as-is (no coverage loss, low-value mechanical move, out of this pass's scope). |
| generator-exec "promises from JS functions" (mislabeled) | cross-form lambda persistence | renamed row in the same file. **ALREADY EXECUTED** (generator-exec.spec.ts's describe now reads "cross-form lambda persistence"; re-verified 2026-07-09, no action needed). |
| r7rs-unicode/-identity "known bugs (it.fails…)" describe titles | nothing — bugs fixed, labels stale | retitled "FIXED at …" in place (files survive). **ALREADY EXECUTED** (both files' headers confirm the "FIXED at ..." convention landed; re-verified 2026-07-09, no action needed). |
| r7rs num()/truthy() boxed-or-raw helpers | masked the exit-convention inconsistency | gone after R1: helpers replaced by the single exit contract. **ALREADY EXECUTED**: no `truthy()` helper exists anywhere in the package; `helpers.ts`'s `num()` is a plain fixture CONSTRUCTOR (builds a boxed AExact/AInexact for test inputs), not a boxed-or-raw comparison-tolerance helper — the described masking helper is gone, re-verified 2026-07-09 via full-repo grep. |
| membrane.spec symbol quoted-name green pin (todo-in-comment) | the opaque-symbol gap | `it.fails`/`it.todo` per the opaque-symbol design. **EXECUTED 2026-07-09 (G2)**: `membrane.spec.ts`'s "converts SchemeSymbol via its own toJS protocol" promoted from a green pin (with a stale "todo symbols gets transformed into opaque symbol" comment) to `it.todo`, citing the same deferred opaque-symbol-exit design already staged as `it.todo` in `membrane/crossing.law.test.ts`'s "registered symbol" case (two-tier-exec-api.md §9). Verified 38 passed/1 todo green. |
| rosetta-pure-marker inline-assembled Classifier consumer | pure/pipe classification semantics | `provenance/` law rows + a P14 staging ledger naming the production wiring gate. **NOT EXECUTED — reported as a doubt, not force-created**: `rosetta-pure-marker.test.ts` already self-documents the exact gap in detail (`[STAGING: rosetta-pure-marker]` comment, P14-cited) — a real `provenance/` law row + formal `ledger/index.law.test.ts` staging entry were NOT created this pass (would be a from-scratch design of the production-wiring gate's shape, not a mechanical carry-forward like the other rows here). Left as a named follow-up rather than fabricated to close the checkbox. |
| lists-contract "is_pair-shadow swap byte-identical" spot-checks | helper-equivalence impl-pin | gone (cyclic-list-ops + chibi cover the behavior). **EXECUTED 2026-07-09 (G2)**: deleted the "behavior spot-checks: is_pair-shadow swap..." describe block from `lists-contract-precision.test.ts` (its own comment already named the replacement suite); left a one-line pointer comment in its place. Verified: same 6 pre-existing unrelated failures as the pre-edit baseline, no new failures, no failures from the deleted block. |
| curly-infix flag-on suite (~40) | SRFI-105 semantics behind an unwired flag | R6 RULED: n-expressions force-eliminated — the reader's curly-infix MODE is deleted; `{a * b}` gets an explicit BAN door (educational, points at dict-literal grammar + sugarcoat); suite shrinks to ban-door + dict-grammar rows in `doors/`; neoteric syntax lives in arrival-sugarcoat only. **ALREADY EXECUTED** (`src/reader/curly-infix.ts` and `src/__tests__/curly-infix.test.ts` confirmed absent from source; `doors/infix-ban.law.test.ts` is the live survivor; re-verified 2026-07-09, no action needed). |
| crossing.law "egress of deferred carriers" block (3 `it.fails`) | the force-on-egress contract for a live AHalfBaked crossing exec's boundary | gone: AHalfBaked dissolved (halfbaked-existence-review.md, VERDICT KILL) — the gap became UNREACHABLE, not fixed (no carrier can exist, so there is nothing left to force); ledger GAPS row "live AHalfBaked escapes exec under speculate" retired to a comment. **ALREADY EXECUTED** (crossing.law.test.ts's own trailing comment confirms this; re-verified 2026-07-09, no action needed). |

## C. Retagged in place (not removed — expiry-tagged)

All 25 RETAG rows from VERDICTS.md get standard markers and stay until their gate:
`[INVERTS: bare-value-purge/P4]`, `[INVERTS: reverse-membrane/P1]`,
`[INVERTS: region-discipline/P6]`, `[STALE-LABEL]` fixes, `[STAGING]` ledgers.
The v2 `ledger/` suite owns the gate index: one table mapping every tag → the migration that
flips it → the law row that replaces it.

## D. Explicitly kept as-is (positive controls)

tail-call, oracle-contract, golden-prov-infer/special-forms, capability-rosetta-symbol,
env-pack (C3 vs Python MRO), name-escape, query.test.ts, prelude/diagnose, let-bracket +
cond-case-do door suites, syntax-rules trio, parser.test, sandbox-boundary, both
polyglot-rich-errors suites, all contract-precision suites, the algebra law-harness files,
purity-doors, provenance-algebra.property, tap.spec, collapse-provenance,
provenance-deep-stamp, attestation, abort, escaped-symbols, srfi-13/28,
comparison-divergence, boolean-landmine (verified non-inverting), lineage suite (staged via
G-gates), scheme-zod suites minus the flagged rows.
