> **Historical record — executed.** Findings below carry their per-row EXECUTED/SKIPPED markers (G1 sweep `db850bab44`); rulings resolved in `../../test-suite-v2/RULINGS.md`; the v1 suite was subsequently retired (G2, `5d4919ad8f`). The LAMBDA brand named below is since deleted (B5, `a484d7c1ab`).

## Findings

`membrane.spec.ts > toJS > "converts SchemeSymbol via its own protocol to a quoted-name string ('foo)"` — [P15] RETAG: comment admits `// todo symbols gets transformed into opaque symbol` (not final), yet pinned via plain `it()` not `it.fails`/`it.todo`, while js-interop's sibling gap (schemeToJs(symbol) should unwrap to bare string) is honestly `it.fails` — same underlying gap, inconsistent taxonomy. **SKIPPED** — not in the 2026-07-08 mechanical-sweep task list; untouched.

`membrane-symmetry.test.ts > "a LAMBDA-branded function passes through jsToScheme by identity"` — [P1] RETAG: P1's own "Revealed by" names this exact test as evidence of a JS artifact living in value space without lineage; comment frames it as correct-by-fiat but it's the transitional legacy shape, not the target state. **EXECUTED** (2026-07-08 mechanical sweep — `[INVERTS: reverse-membrane/P1]` marker added).

`membrane-symmetry.test.ts > "isSchemeValue completeness — every native AValue subtype is recognised"` — [P7] RULING-NEEDED: whole describe pins an external instanceof-enumeration that must be kept in sync per-subtype (own comment: "Nil clone... deliberately kept here for the completeness map," duplicating membrane.ts:71); this is the split-authority pattern P7 targets for conversions — unclear if a type-guard predicate is exempt or should route through a protocol marker instead. **SKIPPED** — RULING-NEEDED, not part of the mechanical sweep; untouched.

`js-interop.test.ts > "JS-interop: strings & booleans" > "strings come back as AStrings..."` — [P4] RULING-NEEDED: pins boxed-out exit contract for strings; paired directly against the next finding per P4's own cited contradiction. **SKIPPED** — RULING-NEEDED, not part of the mechanical sweep; untouched.

`js-interop.test.ts > "JS-interop: strings & booleans" > "booleans come back as raw JS booleans"` — [P4] RULING-NEEDED: pins raw-out exit contract for booleans. Verified in source (`op-helpers.ts` `withInputProvenance`/`deriveOrd`/`looseCompare`): a comparison over empty-provenance operands short-circuits boxing and returns the bare JS boolean, while strings/numbers stay boxed through the same exec path — a real, load-bearing asymmetry, not test error. This pair is P4's textbook "two invariants pinning opposite exit contracts." **SKIPPED** — RULING-NEEDED, not part of the mechanical sweep; untouched.

`js-interop.test.ts > "numbers" > "exact numbers do NOT JSON.stringify today — BigInt-backed throws (documented current behavior)"` — [P15] FLIP-TO-FAILS: current-broken behavior (BigInt throw) asserted green via plain `it()`; comment even says "when fixed this stops throwing and the test goes red" — that's exactly the semantics `it.fails` exists for, and its inexact sibling in the same file is correctly `it.fails`. **EXECUTED** (2026-07-08 mechanical sweep — flipped to `it.fails` asserting `JSON.stringify(n) === "3"`).

`js-interop.test.ts > "lists (Pair)" > "JSON.stringify(list) throws today — BigInt elements"` — [P15] FLIP-TO-FAILS: same root-cause bug (BigInt backing), same "documented current behavior" pattern, same fix — should be `it.fails` not `it()`. **EXECUTED** (2026-07-08 mechanical sweep — flipped to `it.fails` asserting `JSON.stringify(lst) === "[1,2,3]"`).

`contract-precision-fixes.test.ts > "boolean=?: input STAYS z.array(z.unknown())..."` — [P4] RULING-NEEDED: explicitly asserts BOTH boxed ABool and raw JS boolean must be accepted as valid input — literally the "accepts boxed or raw" contract P4's Forbids list names, blessed here as "load-bearing, not imprecision." Same underlying design tension as the two js-interop findings above; needs one ruling across all three. **SKIPPED** — RULING-NEEDED, not part of the mechanical sweep; untouched.

`rosetta-environment.test.ts > "Environment.defineRosetta" > both tests` — [P1] RETAG: exercises the legacy `env.defineRosetta` arm P1 names directly in its "Revealed by" line ("env.defineRosetta's legacy form") as a JS artifact in value space without lineage. **EXECUTED** (2026-07-08 mechanical sweep — `[INVERTS: reverse-membrane/P1]` marker added above the describe, covering all 3 tests in the block).

`rosetta-environment.test.ts > "Real-world Use Cases" > both tests` — [P1] RETAG: same legacy `defineRosetta` arm (MCP CSS-filtering / stats patterns) — should travel with the same transitional tag as the block above rather than read as a permanent contract. **EXECUTED** (2026-07-08 mechanical sweep — `[INVERTS: reverse-membrane/P1]` marker added above the describe).

`rosetta-environment.test.ts > "should convert empty LIPS list to empty JS array" (it.skip)` — [P15] RETAG: fully-bodied test with real assertions marked `it.skip`, not `it.todo` — sits outside the suite's declared three-state truth table (green/`it.fails`/`it.todo`); atlas itself classifies it as `[todo]` even though the code says `skip`. **EXECUTED** (2026-07-08 mechanical sweep — promoted `it.skip` → `it.todo`; verified vitest 4 accepts a body on `it.todo` without executing it).

`rosetta-pure-marker.test.ts > entire file` — [P14] RETAG: honestly self-labeled ("starting-point foothold... richer taxonomy deferred") but the `Classifier` consumed in the test is assembled inline in the test itself, not read from any production wiring — no `it.todo`/gate-ledger the way P14's model case (the static lineage classifier) is staged; should get a formal staging marker rather than reading as a shipped classification. **EXECUTED** (2026-07-08 mechanical sweep — `[STAGING: rosetta-pure-marker]` marker added to the file header).

`sandbox-escape.test.ts > "resource exhaustion" > "(equal? a b) on cyclic structures does not throw native JSON error"` — [P16] REWRITE: accepts EITHER outcome (returns-a-boolean OR throws-with-safe-message) as passing — the exact "vacuous both-outcomes-pass assertion" P16 forbids. `equal?` on cyclic input should commit to one behavior. **EXECUTED** (2026-07-08 mechanical sweep — ran it: two independently-cyclic JS objects compare `equal?` → boxed `#f`, never throws; rewrote to assert exactly that single outcome, `String(result) === "#f"`).

`sandbox-escape.test.ts > "resource exhaustion" > "deeply-nested input throws a graceful parse error, not stack overflow"` — [P5/P16] RULING-NEEDED: only asserts the message does NOT match `/Maximum call stack/`; comment even proposes the ideal (`/nest|depth|too deep/i`) but never asserts it — doesn't verify the door "teaches" per P5. **EXECUTED** (2026-07-08 mechanical sweep, bundled into the task's sandbox-escape REWRITE bullet — ran it: current message is "input nesting depth exceeded 2000 at 1:2000", already teaches; added the positive assertion `toMatch(/nest|depth|too deep/i)` alongside the existing negative one).

`sandbox-escape.test.ts > "resource exhaustion" > "(make-string 1e8...)" and "(make-vector 1e8...)"` — [P5] RULING-NEEDED: both only assert `caught===true` + timing, never that the thrown error is the intended cap-policy door (vs. an engine RangeError) — same weak-assertion pattern as above, repeated. **EXECUTED** (2026-07-08 mechanical sweep, bundled into the task's sandbox-escape REWRITE bullet — ran both: actual thrown messages are "make-string: requested length 100000000 exceeds allocation limit 16777216" and the make-vector analogue (op-helpers.ts's `assertAllocatable`); rewrote both to assert that message shape instead of bare `caught===true`).

`sandbox-escape.test.ts > "eval defaults to sandbox env, NOT global, when no env arg"` — [P16] RETAG: test name/comments narrate an env-fallback policy that no longer applies (`eval` was deleted outright); the actual assertion is just "Unbound." Harmless but pins a stale internal narrative rather than current behavior. **EXECUTED** (2026-07-08 mechanical sweep — `[STALE-LABEL]` marker added, test left as a regression guard that `eval` stays gone).

`dataflow-thesis-probes.test.ts > "ATTRIBUTION" > "MEASURE: count provenance for two different element-id sets"` — [P15] RULING-NEEDED: green `toMatchInlineSnapshot` locks current entangled-provenance behavior while the file's own comment states the minimal-cone thesis and the teleological-sealing decision are "OPPOSITE goals" and "the conflict is V's call" — a green test whose point is an undecided design question, not an intended behavior. **SKIPPED** — RULING-NEEDED, not part of the mechanical sweep; untouched.

## Clean

- `membrane.spec.ts` — all `fromJS`/`toJS`/`SchemeJSObject`/roundtrip tests, including the bytevector/Promise raw-passthrough in `fromJS`: verified in source (`membrane.ts`'s `FromJSResult` type + comment block) this is a deliberately named, documented superset ("polymorphic bytevector ops rather than an owned ABytevector copy"), not a silent P4/P5 violation. The "refuses an already-boxed value" tests are correctly the 2026-07-08 rebaseline per the atlas note.
- `sandbox-boundary.spec.ts` — thorough, mechanical unit coverage of `accessMember`/`accessSet`/`accessHas`/`accessKeys`/boundary marking; no P0-P16 tension found.
- `sandbox-unification.test.ts` — small, honest non-existence guard; clean.
- `oracle-contract.spec.ts` — exemplary P15 coherence-law suite (Layer-S vs. inlined canonical reference, session-vs-analyze agreement); no issues.
- `polyglot-rich-errors-typo.test.ts` / `polyglot-rich-errors-stubs.test.ts` — well-built errors-as-doors suites with real message/redirect assertions; no vacuity or impl-pin abuse beyond what the atlas already tags.
- `contract-precision-fixes.test.ts` — all tests except the flagged `boolean=?` one are straightforward contract-precision proofs; clean.
- `sandbox-escape.test.ts` — remainder of the file (accessor isolation, registry poisoning, prototype pollution, interned-symbol heap-bounding) is sound; only the four items above are flagged.

## Counts

- Findings: 17 (RULING-NEEDED: 9, RETAG: 6, FLIP-TO-FAILS: 2, REWRITE: 1, DELETE: 0)
- Files with findings: 6 of 13 (js-interop.test.ts, membrane.spec.ts, membrane-symmetry.test.ts, rosetta-environment.test.ts, rosetta-pure-marker.test.ts, sandbox-escape.test.ts, dataflow-thesis-probes.test.ts, contract-precision-fixes.test.ts — 8, correcting count)
- Files fully clean: 5 (sandbox-boundary.spec.ts, sandbox-unification.test.ts, oracle-contract.spec.ts, polyglot-rich-errors-typo.test.ts, polyglot-rich-errors-stubs.test.ts)
