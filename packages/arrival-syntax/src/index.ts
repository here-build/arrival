// @inhuman.tools/arrival-syntax — span-preserving Scheme s-expression forest.
// Leaf: parseSexprs + Node. Not the evaluator reader; not the sugarcoat lens.

export { parseSexprs, type ListOpen, type Node } from "./parse.js";
