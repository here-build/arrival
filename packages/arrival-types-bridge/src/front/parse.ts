/**
 * Scheme s-expr forest — owned by `@inhuman.tools/arrival-syntax`.
 * Re-exported so the types-bridge front stays one import path.
 */
export { parseSexprs, type ListOpen, type Node } from "@inhuman.tools/arrival-syntax";
