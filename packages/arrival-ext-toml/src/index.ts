// arrival-ext-toml — the `.toml` require resolver capability; owns the smol-toml parser.
// Decomposed from arrival-chain packs/ (2026-07-06).
export { tomlHandler, arrivalTomlCapability } from "./ext-toml.js";
