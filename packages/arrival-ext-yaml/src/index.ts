// arrival-ext-yaml — the `.yaml`/`.yml` require resolver capability; owns the yaml parser.
// Decomposed from arrival-chain packs/ (2026-07-06).
export { yamlHandler, arrivalYamlCapability } from "./ext-yaml.js";
