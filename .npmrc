# Dual-mode: monorepo supplies arrival-run / llm-plane-* from inhuman/foundations.
# Standalone extract must not auto-install those unpublished peers from npm.
auto-install-peers=false
strict-peer-dependencies=false
